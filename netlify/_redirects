# Redirects and rewrites for Netlify
# https://docs.netlify.com/routing/redirects/

# Rewrites for Next.js pages
/*              /.netlify/functions/next/:splat        200

# Force HTTPS
http://*        https://:splat                         301!

# Redirect default Netlify subdomain to primary domain
# Uncomment and replace with your domains when ready
# https://itrade2g-nextjs.netlify.app/*  https://yourdomain.com/:splat  301!